rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura a cualquier usuario (modo desarrollo)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}